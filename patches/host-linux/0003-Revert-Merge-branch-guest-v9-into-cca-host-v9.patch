From 63a1b535f6c87e5a54b605e53d023412fc20e61b Mon Sep 17 00:00:00 2001
From: "Al Sadi, Amir" <a.al-sadi@imperial.ac.uk>
Date: Tue, 21 Oct 2025 13:32:19 +0000
Subject: [PATCH 03/14] Revert "Merge branch 'guest/v9' into 'cca-host/v9'"

This reverts merge request !1
---
 arch/arm64/include/asm/rsi_cmds.h | 18 ------------------
 arch/arm64/include/asm/rsi_smc.h  |  9 ---------
 arch/arm64/kernel/rsi.c           |  2 --
 3 files changed, 29 deletions(-)

diff --git a/arch/arm64/include/asm/rsi_cmds.h b/arch/arm64/include/asm/rsi_cmds.h
index 8fed521ce3e0..2c8763876dfb 100644
--- a/arch/arm64/include/asm/rsi_cmds.h
+++ b/arch/arm64/include/asm/rsi_cmds.h
@@ -22,16 +22,6 @@ enum ripas {
 	RSI_RIPAS_DEV = 3,
 };
 
-static inline void invoke_rsi_fn_smc_with_res(unsigned long function_id,
-                                              unsigned long arg0,
-                                              unsigned long arg1,
-                                              unsigned long arg2,
-                                              unsigned long arg3,
-                                              struct arm_smccc_res *res)
-{
-    arm_smccc_smc(function_id, arg0, arg1, arg2, arg3, 0, 0, 0, res);
-}
-
 static inline unsigned long rsi_request_version(unsigned long req,
 						unsigned long *out_lower,
 						unsigned long *out_higher)
@@ -57,14 +47,6 @@ static inline unsigned long rsi_get_realm_config(struct realm_config *cfg)
 	return res.a0;
 }
 
-static inline unsigned long rsi_custom_print(unsigned long req)
-{
-	struct arm_smccc_res res;
-
-	arm_smccc_smc(SMC_RSI_CUSTOM_PRINT, req, 0, 0, 0, 0, 0, 0, &res);
-	return res.a0;
-}
-
 static inline unsigned long rsi_ipa_state_get(phys_addr_t start,
 					      phys_addr_t end,
 					      enum ripas *state,
diff --git a/arch/arm64/include/asm/rsi_smc.h b/arch/arm64/include/asm/rsi_smc.h
index 420a310c81fd..6cb070eca9e9 100644
--- a/arch/arm64/include/asm/rsi_smc.h
+++ b/arch/arm64/include/asm/rsi_smc.h
@@ -190,13 +190,4 @@ struct realm_config {
  */
 #define SMC_RSI_HOST_CALL			SMC_RSI_FID(0x199)
 
-
-/*
- * Custom print from within a Realm.
- *
- * ret0 == Status / error
- */
-#define SMC_RSI_CUSTOM_PRINT                     SMC_RSI_FID(0x1B0)
-
-
 #endif /* __ASM_RSI_SMC_H_ */
diff --git a/arch/arm64/kernel/rsi.c b/arch/arm64/kernel/rsi.c
index 57160ea84ee1..ce4778141ec7 100644
--- a/arch/arm64/kernel/rsi.c
+++ b/arch/arm64/kernel/rsi.c
@@ -126,8 +126,6 @@ void __init arm64_rsi_init(void)
 		return;
 	if (!rsi_version_matches())
 		return;
-	if (rsi_custom_print(1) != RSI_SUCCESS)
-		return;
 	if (WARN_ON(rsi_get_realm_config(&config)))
 		return;
 	prot_ns_shared = BIT(config.ipa_bits - 1);
-- 
2.34.1

