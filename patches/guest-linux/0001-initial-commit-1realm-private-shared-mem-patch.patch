From 184d7f9e11a5db0ecc314624a714f118deb4a03c Mon Sep 17 00:00:00 2001
From: Amir Al Sadi <alsadi.amir@gmail.com>
Date: Tue, 21 Oct 2025 13:36:52 +0100
Subject: [PATCH 01/18] initial commit + 1realm private shared mem patch

---
 arch/arm64/include/asm/kvm_rme.h  |   7 ++
 arch/arm64/include/asm/rmi_cmds.h |  36 +++++++++
 arch/arm64/include/asm/rmi_smc.h  |   3 +
 arch/arm64/include/uapi/asm/kvm.h |   2 +
 arch/arm64/kvm/mmu.c              | 123 ++++++++++++++++++++++--------
 arch/arm64/kvm/rme.c              | 109 ++++++++++++++++++++++++++
 include/linux/kvm_host.h          |  20 +++++
 include/uapi/linux/kvm.h          |   2 +
 virt/kvm/kvm_main.c               |   3 +-
 9 files changed, 270 insertions(+), 35 deletions(-)

diff --git a/arch/arm64/include/asm/kvm_rme.h b/arch/arm64/include/asm/kvm_rme.h
index e954bb95dc86..7cd6a70ec5ec 100644
--- a/arch/arm64/include/asm/kvm_rme.h
+++ b/arch/arm64/include/asm/kvm_rme.h
@@ -121,6 +121,11 @@ int realm_map_protected(struct realm *realm,
 			kvm_pfn_t pfn,
 			unsigned long size,
 			struct kvm_mmu_memory_cache *memcache);
+int realm_map_protected_shared(struct realm *realm,
+			unsigned long base_ipa,
+			kvm_pfn_t pfn,
+			unsigned long size,
+			struct kvm_mmu_memory_cache *memcache);
 int realm_map_non_secure(struct realm *realm,
 			 unsigned long ipa,
 			 kvm_pfn_t pfn,
@@ -136,4 +141,6 @@ static inline bool kvm_realm_is_private_address(struct realm *realm,
 	return !(addr & BIT(realm->ia_bits - 1));
 }
 
+int kvm_realm_prealloc_rtt_range(struct kvm *kvm, u64 ipa, u64 size, u32 target_level);
+
 #endif /* __ASM_KVM_RME_H */
diff --git a/arch/arm64/include/asm/rmi_cmds.h b/arch/arm64/include/asm/rmi_cmds.h
index ef53147c1984..fede0d7d7c02 100644
--- a/arch/arm64/include/asm/rmi_cmds.h
+++ b/arch/arm64/include/asm/rmi_cmds.h
@@ -41,6 +41,25 @@ static inline int rmi_data_create(unsigned long rd, unsigned long data,
 	return res.a0;
 }
 
+/**
+ * rmi_data_create_unknown() - Create a data granule with unknown contents
+ * @rd: PA of the RD
+ * @data: PA of the target granule
+ * @ipa: IPA at which the granule will be mapped in the guest
+ *
+ * Return: RMI return code
+ */
+static inline int rmi_data_create_unknown_shared(unsigned long rd,
+					  unsigned long data,
+					  unsigned long ipa)
+{
+	struct arm_smccc_res res;
+
+	arm_smccc_1_1_invoke(SMC_RMI_DATA_CREATE_UNKNOWN_SHARED, rd, data, ipa, &res);
+
+	return res.a0;
+}
+
 /**
  * rmi_data_create_unknown() - Create a data granule with unknown contents
  * @rd: PA of the RD
@@ -183,6 +202,23 @@ static inline int rmi_realm_activate(unsigned long rd)
 	return res.a0;
 }
 
+/**
+ * rmi_realm_custom_print() - Print a realm custom message
+ * @rd: PA of the RD
+ *
+ * A debug print function to log on console.
+ *
+ * Return: RMI return code
+ */
+// static inline int rmi_realm_custom_print(unsigned long rd)
+// {
+// 	struct arm_smccc_res res;
+
+// 	arm_smccc_1_1_invoke(SMC_RMI_CUSTOM_PRINT, rd, &res);
+
+// 	return res.a0;
+// }
+
 /**
  * rmi_realm_create() - Create a realm
  * @rd: PA of the RD
diff --git a/arch/arm64/include/asm/rmi_smc.h b/arch/arm64/include/asm/rmi_smc.h
index 504009a42035..8263808cdcc4 100644
--- a/arch/arm64/include/asm/rmi_smc.h
+++ b/arch/arm64/include/asm/rmi_smc.h
@@ -45,6 +45,8 @@
 #define SMC_RMI_RTT_INIT_RIPAS		SMC_RMI_CALL(0x0168)
 #define SMC_RMI_RTT_SET_RIPAS		SMC_RMI_CALL(0x0169)
 
+#define SMC_RMI_DATA_CREATE_UNKNOWN_SHARED		SMC_RMI_CALL(0x016a)
+
 #define RMI_ABI_MAJOR_VERSION	1
 #define RMI_ABI_MINOR_VERSION	0
 
@@ -64,6 +66,7 @@
 #define RMI_ERROR_REALM		2
 #define RMI_ERROR_REC		3
 #define RMI_ERROR_RTT		4
+#define RMI_ERROR_SHARED_RANGE		5
 
 enum rmi_ripas {
 	RMI_EMPTY = 0,
diff --git a/arch/arm64/include/uapi/asm/kvm.h b/arch/arm64/include/uapi/asm/kvm.h
index 9b5d67ecbc5e..584fc325e6b3 100644
--- a/arch/arm64/include/uapi/asm/kvm.h
+++ b/arch/arm64/include/uapi/asm/kvm.h
@@ -436,6 +436,8 @@ enum {
 #define KVM_CAP_ARM_RME_INIT_RIPAS_REALM	2
 #define KVM_CAP_ARM_RME_POPULATE_REALM		3
 #define KVM_CAP_ARM_RME_ACTIVATE_REALM		4
+#define KVM_CAP_ARM_RME_CUSTOM_PRINT        5
+
 
 /* List of configuration items accepted for KVM_CAP_ARM_RME_CONFIG_REALM */
 #define ARM_RME_CONFIG_RPV			0
diff --git a/arch/arm64/kvm/mmu.c b/arch/arm64/kvm/mmu.c
index c866891fd8f9..dbb957f00565 100644
--- a/arch/arm64/kvm/mmu.c
+++ b/arch/arm64/kvm/mmu.c
@@ -1523,13 +1523,39 @@ static int realm_map_ipa(struct kvm *kvm, phys_addr_t ipa,
 
 	ipa = ALIGN_DOWN(ipa, PAGE_SIZE);
 
-	if (!kvm_realm_is_private_address(realm, ipa))
+	if (!kvm_realm_is_private_address(realm, ipa)){
+		// pr_info("Realm mapping non-private IPA 0x%llx with IPA bits: 0x%u\n",
+		// 	(unsigned long long)ipa, realm->ia_bits);
 		return realm_map_non_secure(realm, ipa, pfn, map_size,
 					    memcache);
-
+	}
+	// pr_info("Realm mapping private IPA 0x%llx with IPA bits: 0x%u\n",
+	// 		(unsigned long long)ipa, realm->ia_bits);
 	return realm_map_protected(realm, ipa, pfn, map_size, memcache);
 }
 
+static int realm_map_ipa_protected_shared(struct kvm *kvm, phys_addr_t ipa, //continue here
+			 kvm_pfn_t pfn, unsigned long map_size,
+			 enum kvm_pgtable_prot prot,
+			 struct kvm_mmu_memory_cache *memcache)
+{
+	struct realm *realm = &kvm->arch.realm;
+
+	/*
+	 * Write permission is required for now even though it's possible to
+	 * map unprotected pages (granules) as read-only. It's impossible to
+	 * map protected pages (granules) as read-only.
+	 */
+	if (WARN_ON(!(prot & KVM_PGTABLE_PROT_W)))
+		return -EFAULT;
+
+	ipa = ALIGN_DOWN(ipa, PAGE_SIZE);
+
+	// pr_info("Realm mapping private shared IPA 0x%llx with IPA bits: 0x%u\n",
+	// 		(unsigned long long)ipa, realm->ia_bits);
+	return realm_map_protected_shared(realm, ipa, pfn, map_size, memcache);
+}
+
 static int private_memslot_fault(struct kvm_vcpu *vcpu,
 				 phys_addr_t fault_ipa,
 				 struct kvm_memory_slot *memslot)
@@ -1538,48 +1564,73 @@ static int private_memslot_fault(struct kvm_vcpu *vcpu,
 	gpa_t gpa = kvm_gpa_from_fault(kvm, fault_ipa);
 	gfn_t gfn = gpa >> PAGE_SHIFT;
 	bool is_priv_gfn = kvm_mem_is_private(kvm, gfn);
+	bool is_priv_sh_gfn = kvm_mem_is_private_shared(kvm, gfn);
+	// bool is_priv_sh_committed = kvm_mem_is_private_shared_committed(kvm, gfn);
 	struct kvm_mmu_memory_cache *memcache = &vcpu->arch.mmu_page_cache;
 	struct page *page;
 	kvm_pfn_t pfn;
 	int ret;
-	/*
-	 * For Realms, the shared address is an alias of the private GPA with
-	 * the top bit set. Thus is the fault address matches the GPA then it
-	 * is the private alias.
-	 */
-	bool is_priv_fault = (gpa == fault_ipa);
 
-	if (is_priv_gfn != is_priv_fault) {
-		kvm_prepare_memory_fault_exit(vcpu, gpa, PAGE_SIZE,
-					      kvm_is_write_fault(vcpu), false,
-					      is_priv_fault);
+	// if(is_priv_sh_committed) {
+	// 	return 1;
+	// } else 
+	if (is_priv_sh_gfn) {
+		ret = kvm_mmu_topup_memory_cache(memcache,
+						kvm_mmu_cache_min_pages(vcpu->arch.hw_mmu));
+		if (ret)
+			return ret;
 
+		ret = kvm_gmem_get_pfn(kvm, memslot, gfn, &pfn, &page, NULL);
+		if (ret)
+			return ret;
+
+		u32 prot = KVM_PGTABLE_PROT_R | KVM_PGTABLE_PROT_W;
+
+		/* FIXME: Should be able to use bigger than PAGE_SIZE mappings */
+		ret = realm_map_ipa_protected_shared(kvm, fault_ipa, pfn, PAGE_SIZE, prot,
+					memcache);
+		if (!ret)
+			return 1; /* Handled */
+	} else {
 		/*
-		 * KVM_EXIT_MEMORY_FAULT requires an return code of -EFAULT,
-		 * see the API documentation
-		 */
-		return -EFAULT;
-	}
+		* For Realms, the shared address is an alias of the private GPA with
+		* the top bit set. Thus is the fault address matches the GPA then it
+		* is the private alias.
+		*/
+		bool is_priv_fault = (gpa == fault_ipa);
+
+		if (is_priv_gfn != is_priv_fault) {
+			kvm_prepare_memory_fault_exit(vcpu, gpa, PAGE_SIZE,
+							kvm_is_write_fault(vcpu), false,
+							is_priv_fault);
+
+			/*
+			* KVM_EXIT_MEMORY_FAULT requires an return code of -EFAULT,
+			* see the API documentation
+			*/
+			return -EFAULT;
+		}
 
-	if (!is_priv_fault) {
-		/* Not a private mapping, handling normally */
-		return -EINVAL;
-	}
+		if (!is_priv_fault) {
+			/* Not a private mapping, handling normally */
+			return -EINVAL;
+		}
 
-	ret = kvm_mmu_topup_memory_cache(memcache,
-					 kvm_mmu_cache_min_pages(vcpu->arch.hw_mmu));
-	if (ret)
-		return ret;
+		ret = kvm_mmu_topup_memory_cache(memcache,
+						kvm_mmu_cache_min_pages(vcpu->arch.hw_mmu));
+		if (ret)
+			return ret;
 
-	ret = kvm_gmem_get_pfn(kvm, memslot, gfn, &pfn, &page, NULL);
-	if (ret)
-		return ret;
+		ret = kvm_gmem_get_pfn(kvm, memslot, gfn, &pfn, &page, NULL);
+		if (ret)
+			return ret;
 
-	/* FIXME: Should be able to use bigger than PAGE_SIZE mappings */
-	ret = realm_map_ipa(kvm, fault_ipa, pfn, PAGE_SIZE, KVM_PGTABLE_PROT_W,
-			    memcache);
-	if (!ret)
-		return 1; /* Handled */
+		/* FIXME: Should be able to use bigger than PAGE_SIZE mappings */
+		ret = realm_map_ipa(kvm, fault_ipa, pfn, PAGE_SIZE, KVM_PGTABLE_PROT_W,
+					memcache);
+		if (!ret)
+			return 1; /* Handled */
+	}
 
 	put_page(page);
 	return ret;
@@ -2104,11 +2155,17 @@ int kvm_handle_guest_abort(struct kvm_vcpu *vcpu)
 	if (ret == 0)
 		ret = 1;
 out:
+	// pr_info("kvm_handle_guest_abort[HIT OUT]: vcpu %d handled fault ipa 0x%llx ret %d\n",
+		// vcpu->vcpu_id, (unsigned long long)fault_ipa, ret);
 	if (ret == -ENOEXEC) {
+		pr_info("Injecting #PF for execute fault ipa 0x%llx\n",
+			(unsigned long long)fault_ipa);
 		kvm_inject_pabt(vcpu, kvm_vcpu_get_hfar(vcpu));
 		ret = 1;
 	}
 out_unlock:
+	// pr_info("kvm_handle_guest_abort[HIT OUT UNLOCK]: vcpu %d fault ipa 0x%llx ret %d\n",
+		// vcpu->vcpu_id, (unsigned long long)fault_ipa, ret);
 	srcu_read_unlock(&vcpu->kvm->srcu, idx);
 	return ret;
 }
diff --git a/arch/arm64/kvm/rme.c b/arch/arm64/kvm/rme.c
index a25f57387e3e..c77e70109724 100644
--- a/arch/arm64/kvm/rme.c
+++ b/arch/arm64/kvm/rme.c
@@ -905,6 +905,9 @@ int realm_map_protected(struct realm *realm,
 		}
 
 		ret = rmi_data_create_unknown(rd, phys, ipa);
+		// if (ret == RMI_ERROR_SHARED_RANGE) {
+		// 	ret = rmi_data_create_shared(rd, ipa);
+		// }
 
 		if (RMI_RETURN_STATUS(ret) == RMI_ERROR_RTT) {
 			/* Create missing RTTs and retry */
@@ -959,6 +962,99 @@ int realm_map_protected(struct realm *realm,
 	return -ENXIO;
 }
 
+int realm_map_protected_shared(struct realm *realm,
+			unsigned long ipa,
+			kvm_pfn_t pfn,
+			unsigned long map_size,
+			struct kvm_mmu_memory_cache *memcache)
+{
+	phys_addr_t phys = __pfn_to_phys(pfn);
+	phys_addr_t rd = virt_to_phys(realm->rd);
+	unsigned long base_ipa = ipa;
+	unsigned long size;
+	int map_level = IS_ALIGNED(map_size, RMM_L2_BLOCK_SIZE) ?
+			RMM_RTT_BLOCK_LEVEL : RMM_RTT_MAX_LEVEL;
+	int ret = 0;
+
+	if (WARN_ON(!IS_ALIGNED(map_size, RMM_PAGE_SIZE) ||
+		    !IS_ALIGNED(ipa, map_size)))
+		return -EINVAL;
+
+	if (map_level < RMM_RTT_MAX_LEVEL) {
+		/*
+		 * A temporary RTT is needed during the map, precreate it,
+		 * however if there is an error (e.g. missing parent tables)
+		 * this will be handled below.
+		 */
+		realm_create_rtt_levels(realm, ipa, map_level,
+					RMM_RTT_MAX_LEVEL, memcache);
+	}
+
+	for (size = 0; size < map_size; size += RMM_PAGE_SIZE) {
+		if (rmi_granule_delegate(phys)) {
+			/*
+			 * It's likely we raced with another VCPU on the same
+			 * fault. Assume the other VCPU has handled the fault
+			 * and return to the guest.
+			 */
+			return 0;
+		}
+
+		ret = rmi_data_create_unknown_shared(rd, phys, ipa);
+
+		if (RMI_RETURN_STATUS(ret) == RMI_ERROR_RTT) {
+			/* Create missing RTTs and retry */
+			int level = RMI_RETURN_INDEX(ret);
+
+			WARN_ON(level == RMM_RTT_MAX_LEVEL);
+
+			ret = realm_create_rtt_levels(realm, ipa, level,
+						      RMM_RTT_MAX_LEVEL,
+						      memcache);
+			if (ret)
+				goto err_undelegate;
+
+			ret = rmi_data_create_unknown_shared(rd, phys, ipa);
+		}
+
+		if (WARN_ON(ret))
+			goto err_undelegate;
+
+		phys += RMM_PAGE_SIZE;
+		ipa += RMM_PAGE_SIZE;
+	}
+
+	if (map_size == RMM_L2_BLOCK_SIZE) {
+		ret = fold_rtt(realm, base_ipa, map_level + 1);
+		if (WARN_ON(ret))
+			goto err;
+	}
+
+	return 0;
+
+err_undelegate:
+	if (WARN_ON(rmi_granule_undelegate(phys))) {
+		/* Page can't be returned to NS world so is lost */
+		get_page(phys_to_page(phys));
+	}
+err:
+	while (size > 0) {
+		unsigned long data, top;
+
+		phys -= RMM_PAGE_SIZE;
+		size -= RMM_PAGE_SIZE;
+		ipa -= RMM_PAGE_SIZE;
+
+		WARN_ON(rmi_data_destroy(rd, ipa, &data, &top));
+
+		if (WARN_ON(rmi_granule_undelegate(phys))) {
+			/* Page can't be returned to NS world so is lost */
+			get_page(phys_to_page(phys));
+		}
+	}
+	return -ENXIO;
+}
+
 int realm_map_non_secure(struct realm *realm,
 			 unsigned long ipa,
 			 kvm_pfn_t pfn,
@@ -1250,6 +1346,16 @@ static int kvm_init_ipa_range_realm(struct kvm *kvm,
 	return realm_init_ipa_state(kvm, addr, end);
 }
 
+// static int kvm_custom_print(struct kvm *kvm)
+// {
+// 	struct realm *realm = &kvm->arch.realm;
+
+// 	if (rmi_realm_custom_print(virt_to_phys(realm->rd)))
+// 		return -ENXIO;
+
+// 	return 0;
+// }
+
 static int kvm_activate_realm(struct kvm *kvm)
 {
 	struct realm *realm = &kvm->arch.realm;
@@ -1414,6 +1520,9 @@ int kvm_realm_enable_cap(struct kvm *kvm, struct kvm_enable_cap *cap)
 	case KVM_CAP_ARM_RME_ACTIVATE_REALM:
 		r = kvm_activate_realm(kvm);
 		break;
+	// case KVM_CAP_ARM_RME_CUSTOM_PRINT:
+	// 	r = kvm_custom_print(kvm);
+	// 	break;
 	default:
 		r = -EINVAL;
 		break;
diff --git a/include/linux/kvm_host.h b/include/linux/kvm_host.h
index 3bde4fb5c6aa..3065dbb26187 100644
--- a/include/linux/kvm_host.h
+++ b/include/linux/kvm_host.h
@@ -863,6 +863,8 @@ struct kvm {
 	struct xarray mem_attr_array;
 #endif
 	char stats_id[KVM_STATS_NAME_SIZE];
+	unsigned long ipa_prot_start;
+	unsigned long ipa_prot_size;
 };
 
 #define kvm_err(fmt, ...) \
@@ -2518,6 +2520,11 @@ static inline unsigned long kvm_get_memory_attributes(struct kvm *kvm, gfn_t gfn
 	return xa_to_value(xa_load(&kvm->mem_attr_array, gfn));
 }
 
+static inline unsigned long kvm_get_memory_flags(struct kvm *kvm, gfn_t gfn)
+{
+	return xa_to_value(xa_load(&kvm->mem_attr_array, gfn));
+}
+
 bool kvm_range_has_memory_attributes(struct kvm *kvm, gfn_t start, gfn_t end,
 				     unsigned long mask, unsigned long attrs);
 bool kvm_arch_pre_set_memory_attributes(struct kvm *kvm,
@@ -2530,6 +2537,19 @@ static inline bool kvm_mem_is_private(struct kvm *kvm, gfn_t gfn)
 	return IS_ENABLED(CONFIG_KVM_PRIVATE_MEM) &&
 	       kvm_get_memory_attributes(kvm, gfn) & KVM_MEMORY_ATTRIBUTE_PRIVATE;
 }
+
+static inline bool kvm_mem_is_private_shared(struct kvm *kvm, gfn_t gfn)
+{
+	return IS_ENABLED(CONFIG_KVM_PRIVATE_MEM) &&
+	       kvm_get_memory_attributes(kvm, gfn) & KVM_MEMORY_ATTRIBUTE_PRIVATE_SHARED;
+}
+
+static inline bool kvm_mem_is_private_shared_committed(struct kvm *kvm, gfn_t gfn)
+{
+	return IS_ENABLED(CONFIG_KVM_PRIVATE_MEM) &&
+	       kvm_get_memory_attributes(kvm, gfn) & KVM_MEMORY_ATTRIBUTE_PRIVATE_SHARED_COMMITTED;
+}
+
 #else
 static inline bool kvm_mem_is_private(struct kvm *kvm, gfn_t gfn)
 {
diff --git a/include/uapi/linux/kvm.h b/include/uapi/linux/kvm.h
index 88496dba0f84..bff0893341ce 100644
--- a/include/uapi/linux/kvm.h
+++ b/include/uapi/linux/kvm.h
@@ -1580,6 +1580,8 @@ struct kvm_memory_attributes {
 };
 
 #define KVM_MEMORY_ATTRIBUTE_PRIVATE           (1ULL << 3)
+#define KVM_MEMORY_ATTRIBUTE_PRIVATE_SHARED        (1ULL << 4)
+#define KVM_MEMORY_ATTRIBUTE_PRIVATE_SHARED_COMMITTED      (1ULL << 5)
 
 #define KVM_CREATE_GUEST_MEMFD	_IOWR(KVMIO,  0xd4, struct kvm_create_guest_memfd)
 
diff --git a/virt/kvm/kvm_main.c b/virt/kvm/kvm_main.c
index eec82775c5bf..5ed567cc57b5 100644
--- a/virt/kvm/kvm_main.c
+++ b/virt/kvm/kvm_main.c
@@ -2420,7 +2420,7 @@ static int kvm_vm_ioctl_clear_dirty_log(struct kvm *kvm,
 static u64 kvm_supported_mem_attributes(struct kvm *kvm)
 {
 	if (!kvm || kvm_arch_has_private_mem(kvm))
-		return KVM_MEMORY_ATTRIBUTE_PRIVATE;
+		return KVM_MEMORY_ATTRIBUTE_PRIVATE+KVM_MEMORY_ATTRIBUTE_PRIVATE_SHARED;
 
 	return 0;
 }
@@ -2606,7 +2606,6 @@ static int kvm_vm_ioctl_set_mem_attributes(struct kvm *kvm,
 
 	start = attrs->address >> PAGE_SHIFT;
 	end = (attrs->address + attrs->size) >> PAGE_SHIFT;
-
 	/*
 	 * xarray tracks data using "unsigned long", and as a result so does
 	 * KVM.  For simplicity, supports generic attributes only on 64-bit
-- 
2.34.1

